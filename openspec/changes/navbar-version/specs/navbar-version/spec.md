## ADDED Requirements

### Requirement: Version displayed in nav bar
The nav bar SHALL display the version number after the application name on the right side.

#### Scenario: Version visible
- **WHEN** the TUI is running
- **THEN** the right side of the nav bar SHALL show `dirty-repo-scanner <version>` (e.g., `dirty-repo-scanner 0.2.1`)
