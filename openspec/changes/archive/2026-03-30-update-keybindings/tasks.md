## 1. Key Handling

- [x] 1.1 Change `"e"` case to `"enter"` in Update() for the open/edit action
- [x] 1.2 Add `"j"` alongside `"down"` and `"k"` alongside `"up"` in Update() navigation

## 2. Nav Bar

- [x] 2.1 Update `renderNavBar()` — change `e`/`edit` to `enter`/`open` and `↑↓` to `jk/↑↓`

## 3. Documentation

- [x] 3.1 Update README keybinding table with new keys

## 4. Verification

- [x] 4.1 Run `make build` and `make test`
